export default function Driver(props)
{
    if(props.select!=='1')
        return('');
        return (
        
            <div style={{marginLeft:'300px',marginTop:'100px'}
            }>
           <h1> this will be used for creating driver.</h1>
            </div>
        )
}