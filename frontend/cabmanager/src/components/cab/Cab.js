export default function Cab( props)
{
    if(props.select!=='2')
        return('');
    return (
        
        <div style={{marginLeft:'300px',marginTop:'100px'}
        }>
       <h1> this will be used for creating cab.</h1>
        </div>
    )
}